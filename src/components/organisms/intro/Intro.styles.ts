import { Center, Container, Stack, keyframes } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const Wrapper = styled(Container)``;
export const Content = styled(Stack)``;
export const LeftSideContent = styled(Stack)``;
export const RightSideContent = styled(Center)``;
export const ArrowDownSection = styled(Stack)``;
