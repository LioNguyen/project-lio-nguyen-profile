import { Container, HStack, Stack } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const Wrapper = styled(Container)``;
export const FooterContainer = styled(Stack)``;
export const FooterRow = styled(HStack)`
  gap: 20px;
`;
